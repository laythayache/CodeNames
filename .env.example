# OpenAI API key (optional — omit for free mode with cached questions only)
OPENAI_API_KEY=

# Server port (default: 3000)
PORT=3000

# JWT secret for player tokens (use a long random string in production)
JWT_SECRET=

# CORS origin (default: * for dev, set specific domain in production)
CORS_ORIGIN=
